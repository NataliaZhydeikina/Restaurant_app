/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { FilterArrayOfDishsPipe } from './filter-array-of-dishs.pipe';

describe('Pipe: FilterArrayOfDishs', () => {
  it('create an instance', () => {
    let pipe = new FilterArrayOfDishsPipe();
    expect(pipe).toBeTruthy();
  });
});
